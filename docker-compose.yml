version: "3.9"

services:
  backend:
    build: ./backend
    container_name: tool-homepage-backend
    ports:
      - "8000:8000"
    restart: always

  frontend:
    build: ./frontend
    container_name: tool-homepage-frontend
    ports:
      - "22332:80"
    restart: always
